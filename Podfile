# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'OXXO Integration' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  pod 'Stripe', :git => 'https://github.com/stripe/stripe-ios.git', :branch => 'TEMP-add-oxxo-payment-method-beta-header'
end
